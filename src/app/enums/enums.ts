export enum ACTION {
    HIT = 'hit',
    MISS = 'miss',
}

export enum SHIP_LEN {
    CARRIER = 5,
    BATTLESHIP = 4,
    CRUISER = 3,
    SUBMARINE = 3,
    DESTROYER = 2,
}

export enum SHIP_NAME {
    CARRIER = 'carrier',
    BATTLESHIP = 'battleship',
    CRUISER = 'cruiser',
    SUBMARINE = 'submarine',
    DESTROYER = 'destroyer',
}

export enum GAME {
    WINNING_SCORE = 17,
}