export enum ACTION {
    HIT = 'hit',
    MISS = 'miss',
}

export enum SHIP_LEN {
    CARRIER = 5,
    BATTLESHIP = 4,
    CRUISER = 3,
    SUBMARINE = 3,
    DESTROYER = 2,
}

export enum SHIP_NAME {
    CARRIER = 'Carrier',
    BATTLESHIP = 'Battleship',
    CRUISER = 'Cruiser',
    SUBMARINE = 'Submarine',
    DESTROYER = 'Destroyer',
}

export enum SHIP_SETUP {
    CAR,
    BAT,
    CRU,
    SUB,
    DES,
}